<form method="post" action="{{ path('profil_delete', {'id': user.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir supprimer votre compte ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <div class="row">
    <div class="ml-auto">
      <button class="btn btn-danger mt-3 mr-3">Supprimer
        <i class="fas fa-trash-alt"></i>
      </button>
    </div>
  </div>
</form>
