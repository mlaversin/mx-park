{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="row">
    <div class="ml-auto">
        <button class="btn btn-warning mb-2 mr-3">Sauvegarder <i class="far fa-save"></i></button>
    </div>
    </div>
{{ form_end(form) }}
