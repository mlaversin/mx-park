{% extends 'base.html.twig' %}

{% block stylesheets %}
  <link href="https://www.jqueryscript.net/css/jquerysctipttop.css" rel="stylesheet" type="text/css">
  <link rel="stylesheet" href="css/calendar.css" type="text/css">
{% endblock %}

{% block javascripts %}
  <!-- jquery -->
  <script src="https://code.jquery.com/jquery-3.6.0.slim.min.js" integrity="sha256-u7e5khyithlIdTpu22PHhENmPcRdFiHRjhAuHcs05RI=" crossorigin="anonymous"></script>
  <!-- Popper old version to make tooltips work with bootstrap -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.11.0/umd/popper.min.js"></script>
  <!-- bootstrap.js -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.6.0/js/bootstrap.min.js" integrity="sha512-XKa9Hemdy1Ui3KSGgJdgMyYlUg1gM+QhL6cnlyTe2qzMCYm4nAZ1PsVerQzTTXzonUR+dmswHqgJPuwCq1MaAg==" crossorigin="anonymous"></script>
{% endblock %}

{% block navbarButtons %}
  <li class="nav-item" data-toggle="collapse" data-target=".navbar-collapse.show">
    <a class="nav-link" href="#club" id="navclub">Le Club<span class="sr-only"></span></a>
  </li>
  <li class="nav-item" data-toggle="collapse" data-target=".navbar-collapse.show">
    <a class="nav-link" href="#infos" id="navinfo">Infos</a>
  </li>
  <li class="nav-item" data-toggle="collapse" data-target=".navbar-collapse.show">
    <a class="nav-link" href="#event" id="navevent">Événement</a>
  </li>
  <li class="nav-item mr-3" data-toggle="collapse" data-target=".navbar-collapse.show">
    <a class="nav-link" href="#contact" id="navcontact">Contact</a>
  </li>
{% endblock %}

{% block body %}
  
  <!-- ******** HOMEPAGE ******** -->
  <section id="homepage" class="homepage">

    <!-- Carousel -->
    <div class="carousel slide" id="home-carousel">
      <ul class="carousel-indicators">
        <li data-slide-to="0" data-target="#home-carousel" class="active"></li>
        <li data-slide-to="1" data-target="#home-carousel"></li>
        <li data-slide-to="2" data-target="#home-carousel"></li>
        <li data-slide-to="3" data-target="#home-carousel"></li>
      </ul>
      <div class="carousel-inner">
        <div class="carousel-item active">
          <!-- Version moyens-grands écrans -->
          <img class="img-fluid w-100 carousel-img" src="{{absolute_url(asset("/img/carousel-1.jpg"))}}" alt="">
          <div class="carousel-caption">
            <h3 class="mb-5">
              {% if animation.carouselMessage1 is defined %}
                {{ animation.carouselMessage1 }}
              {% else %}
                Entraînement tous les premiers dimanches du mois !
              {% endif %}
            </h3>
          </div>
        </div>
        <div class="carousel-item">
          <img class="img-fluid w-100 carousel-img" src="{{absolute_url(asset("/img/carousel-2.jpg"))}}" alt="">
          <div class="carousel-caption">
            <h3 class="mb-5">
              {% if animation.carouselMessage2 is defined %}
                {{ animation.carouselMessage2 }}
              {% else %}
                Entraînement tous les premiers dimanches du mois !
              {% endif %}
            </h3>
          </div>
        </div>
        <div class="carousel-item">
          <img class="img-fluid w-100 carousel-img" src="{{absolute_url(asset("/img/carousel-3.jpg"))}}" alt="">
          <div class="carousel-caption">
            <h3 class="mb-5">
              {% if animation.carouselMessage3 is defined %}
                {{ animation.carouselMessage3 }}
              {% else %}
                Entraînement tous les premiers dimanches du mois !
              {% endif %}
            </h3>
          </div>
        </div>
        <div class="carousel-item">
          <img class="img-fluid w-100 carousel-img" src="{{absolute_url(asset("/img/carousel-4.jpg"))}}" alt="">
          <div class="carousel-caption">
            <h3 class="mb-5">
              {% if animation.carouselMessage4 is defined %}
                {{ animation.carouselMessage4 }}
              {% else %}
                Entraînement tous les premiers dimanches du mois !
              {% endif %}
            </h3>
          </div>
        </div>
      </div>
      <a class="carousel-control-prev" href="#home-carousel" data-slide="prev">
        <span class="carousel-control-prev-icon"></span>
        <span class="sr-only">Previous</span>
      </a>
      <a class="carousel-control-next" href="#home-carousel" data-slide="next">
        <span class="carousel-control-next-icon"></span>
        <span class="sr-only">Next</span>
      </a>
    </div>

    <div class="container d-flex align-items-center">
      <div>
        <h1 class="text-center mb-4">Bienvenue au MX Park d'Auribail</h1>
        <p class="h6 text-center mb-3">Situé à 30 minutes de Toulouse, le circuit MX Park Auribail vous accueille tous les premiers dimanches du mois pour des entraînements de motocross !</p>
      </div>
    </div>
  
  </section>

  <!-- ******** PAGE CLUB ********  -->
  <section class="club d-flex flex-column justify-content-center" id="club">
    <h2 class="h1 text-center my-5">Le club en quelques mots</h2>
    <div class="container">
      <div class="row">
        <div class="col-12 col-lg-4 offset-lg-1 text-center">
          <img class="club-illustration" src="./img/club-illustration.jpg" >
        </div>
        <div class="col-12 col-lg-5 text-justify">
          <p>Adrénaline et sensations fortes, vous craquez pour le Motocross ? Auribail MX Park vous accueille tous les premiers dimanches du mois pour des sessions inoubliables !</p>
          <p>Situé à moins de 30 minutes Toulouse, notre club dispose d'un circuit d'entraînement exceptionnel d'une longueur de 1950 mètres par 8 mètres de large avec une terre argileuse de grande qualité.</p>
          <p>Nos entraîneurs mettent à disposition leur savoir et leur expérience pour apprendre la moto du plus jeunes aux plus âgés.</p>
          <p>Alors n'hésitez pas, inscrivez-vous dès maintenant pour la prochaine session !</p>
          <div class="text-center mt-4">
            <a href="#infos" class="btn btn-warning mb-5 px-4">Plus d'infos</a>
          </div>
        </div>
      </div>
    </div>
    
  </section>

  <!-- ******** PAGE INFOS ******** -->
  <section class="infos d-flex flex-column justify-content-center" id="infos">

    <h2 class="h1 text-center my-5">Informations pratiques</h2>

    <div class="container">
      <div class="row">
        <div class="col-12 col-lg-4">
          <h3 class="text-center">Où nous trouver ?</h3>
          <div class="text-center">
            <iframe class="mb-5" src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d63175.51148107438!2d1.340136726550057!3d43.343532766662754!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x12aecfc42536a145%3A0xdde4829e36d4eed4!2sAuribail%20mx%20track!5e0!3m2!1sfr!2sfr!4v1651586425971!5m2!1sfr!2sfr" width="270" height="250" style="border:0;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
          </div>
        </div>
        <div class="col-12 col-lg-8">
          <div class="pt-2">
            <div>
              <h3>Commodités</h3>
              <p>Sanitaires, douches, club house, électricité, terrain technique et école de pilotage sont à disposition.</p>
              <br/>
              <h3>Horaires d'ouverture</h3>
              <p>Le 1er dimanche de chaque mois de 10h à 12h et de 14h à 18h.</p>
              <br/>
            </div>
            <h3>A ne pas oublier</h3>
            <ul>
              <li>Respecter les consignes de sécurité lors de l'utilisation du terrain</li>
              <li>Mettre une tenue réglementaire, y compris un casque aux normes UE</li>
              <li>Avoir une licence
                <strong>à jour</strong>
              </li>
              <li class="mb-5">Venir avec sa bonne humeur :)</li>
            </ul>
          </div>
        </div>
      </div>
    </div>

  </section>

  <!-- ******** PAGE EVENT ******** -->
  <section class="event d-flex flex-column justify-content-center" id="event">
    <h2 class="h1 text-center my-5">Inscription au prochain évènement</h2>
    <div class="container">
      <div class="row">
        <div class="col-12 col-lg-3">
          <div class="icalendar text-center" id='hereData' data-date1='{{ event.0.date|date("Y/m/d") }}' data-date2='{{ kids.0.date|date("Y/m/d") }}' data-link1='{{ event.0.googleCalendarUrl }}' data-link2='{{ kids.0.googleCalendarUrl }}'>
            <div class="icalendar__month">
              <div class="icalendar__prev" onclick="moveDate('prev')">
                <span>&#10094</span>
                <!-- petite flèche vers la gauche -->
              </div>
              <div class="icalendar__current-date">
                <h2 id="icalendarMonth"></h2>
              </div>
              <div class="icalendar__next" onclick="moveDate('next')">
                <span>&#10095</span>
                <!-- petite flèche vers la droite -->
              </div>
            </div>
            <div class="icalendar__week-days">
              <div>Lun</div>
              <div>Mar</div>
              <div>Mer</div>
              <div>Jeu</div>
              <div>Ven</div>
              <div>Sam</div>
              <div>Dim</div>
            </div>
            <div class="icalendar__days"></div>
          </div>
        </div>

        <div class="col-12 col-lg-6">
          <div class="text-center px-3">
            <p class="my-4 h5">Inscrivez-vous à la prochaine session d'entraînement !
            <br>
            Les membres du club sont prioritaires. Les enfants bénéficient d'une session dédiée.
            </p>
            <ul class="text-justify px-5">
              <li>Les inscriptions se font dans la limite des places disponibles.</li>
              <li>Seuls les membres de l'association pourront s'inscrire pendant les
                {% if not currentEvent is empty %}
                  {% set firstDays = currentEvent.startMemberSubs - currentEvent.startAllSubs %}
                  {{firstDays}}
                {% endif %}
                premiers jours.
              <li>Un compte sur le site est nécessaire pour l'inscription.</li>
              <li>Lors de l'inscription, les mineurs seront automatiquement dirigés vers l'entraînement Kid.</li>
            </ul>
          </div>
          <div class="text-center">
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
              {% if isThereSubs is empty %}
                <button class="btn btn-warning px-4 mt-3" data-toggle="modal" data-target="#subsModal">S'inscrire à l'entraînement</button>
              {% else %}
                <button class="btn btn-danger px-4 mt-3" data-toggle="modal" data-target="#unsubsModal">Se désinscrire de l'entraînement</button>
              {% endif %}
            {% else %}
              <button class="btn btn-warning px-4 mt-3" data-toggle="modal" data-target="#authModal">S'inscrire à l'entraînement</button>
            {% endif %}
          </div>
        </div>
        
        <div class="event-gauge col-12 col-lg-3">
          <div class="flex-card flex-gauge my-5 px-3">
            {% set percent = min(event.nbusers, 75) / 75 * 100 %}
            <div class="progress">
              <div class="progress-bar bg-warning" role="progressbar" style="width: {{ percent }}%" aria-valuenow="{{ event.nbusers }}" aria-valuemin="0" aria-valuemax="75"></div>
            </div>
            {% set waiting = max(-1,event.nbusers - 75) %}
            {% if (waiting >= 0) %}
              <p>Entraînement complet,
                {{ waiting }}
                personne(s) sur liste d'attente.</p>
            {% else %}
              <p>{{ event.nbusers }}
                personne(s) inscrite(s) à l'entraînement Adulte.
              </p>
            {% endif %}
          </div>
          <div class="flex-card flex-gauge my-5 px-3">
            {% set percentKids = min(kids.nbusers, 15) / 15 * 100 %}
            <div class="progress">
              <div class="progress-bar bg-warning" role="progressbar" style="width: {{ percentKids }}%" aria-valuenow="{{ kids.nbusers }}" aria-valuemin="0" aria-valuemax="15"></div>
            </div>
            {% set waitingKids = max(-1,kids.nbusers - 15) %}
            {% if (waitingKids >= 0) %}
              <p>Entraînement Kids complet,
                {{ waitingKids }}
                personne(s) sur liste d'attente.</p>
            {% else %}
              <p>{{ kids.nbusers }}
                personne(s) inscrite(s) à l'entraînement Kids.
              </p>
            {% endif %}
          </div>
        </div>
      </div>
    </div>
    
  </section>

  <!-- ******** PAGE CONTACT ******** -->
  <section class="contact d-flex flex-column justify-content-center" id="contact">
    <div class="mx-px-0">
    <div class="container">
        <h1 class="text-center mt-5 mb-3">Contactez-nous</h1>
        <p class="text-center">Besoin d'un renseignement ou d'un conseil ? N'hésitez pas à nous envoyer un message, nous vous répondrons rapidement.</p>

        <div class="row">
          <div class="col-md-6 offset-md-2 mb-md-0 mt-4 mb-3" id="contactForm">
            {{ form_start(formContact) }}
            <div class="row">
              <div class="col-md-6 mb-2">
                {{ form_label(formContact.firstName) }}
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                  {{ form_widget(formContact.firstName, {'value' : app.user.firstName}) }}
                {% else %}
                  {{ form_widget(formContact.firstName) }}
                {% endif %}
              </div>
              <div class="col-md-6 mb-2">
                {{ form_label(formContact.lastName) }}
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                  {{ form_widget(formContact.lastName, {'value' : app.user.lastName}) }}
                {% else %}
                  {{ form_widget(formContact.lastName) }}
                {% endif %}
              </div>
            </div>
            <div class="row">
              <div class="col-md-12 mb-2">
                {{ form_label(formContact.email) }}
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                  {{ form_widget(formContact.email, {'value' : app.user.email}) }}
                {% else %}
                  {{ form_widget(formContact.email) }}
                {% endif %}
              </div>
            </div>
            <div class="row">
              <div class="col-md-12 mb-2">
                {{ form_label(formContact.subject) }}
                {{ form_widget(formContact.subject) }}
              </div>
            </div>
            <div class="row">
              <div class="col-md-12 mb-2">
                {{ form_label(formContact.message) }}
                {{ form_widget(formContact.message) }}
              </div>
            </div>
            {{ form_rest(formContact) }}
            <div class="text-center form-group">
              <button class="btn btn-warning px-4 mt-2 mb-5">Envoyer le message</button>
            </div>
            <div class="status"></div>
            {{ form_end(formContact) }}
          </div>
          <div class="col-md-3 text-center">
            <ul class="list-unstyled mt-5 mb-0">
              <li>
                <i class="fas fa-map-marker-alt fa-2x mt-4"></i>
                <p>Peyret</p>
                <p>31190 AURIBAIL</p>
              </li>
              <li>
                <i class="fas fa-phone mt-4 fa-2x"></i>
                <p>+33 6 88 93 48 15</p>
              </li>
              <li>
                <i class="fas fa-envelope mt-4 fa-2x"></i>
                <p>contact@mxpark.com</p>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ******** FOOTER ******** -->
  <footer class="footer bg-dark text-light py-3 w-100" id="footer">
    <div class="container text-center">
      <div class="footer-copyright">
        Copyright © Motocross Auribail 2021</div>
    </div>
  </footer>

{% endblock %}


{% block modals %}
  <!-- ******** Modal for authentication ******** -->
  <div class="modal fade" id="authModal" tabindex="-1" role="dialog" aria-labelledby="Authentication modal" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">

        <div class="modal-header">
          <h3 class="modal-title" id="exampleModalLabel">Connexion</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>

        <div class="modal-body">
          {{ include('security/login-modal.html.twig') }}
        </div>

        <div class="modal-footer">
          <div class="my-2 text-right">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Annulation</button>
            <button class="btn btn-warning " type="submit">Connexion</button>
          </div>
        </div>

      </form>

    </div>
  </div>
</div>
</div>

<!-- ******** Modal for subscription ******** -->
<div class="modal fade" id="subsModal" tabindex="-1" role="dialog" aria-labelledby="Subscription modal" aria-hidden="true">
<div class="modal-dialog" role="document">
  <div class="modal-content">
    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Inscription</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <div class="container text-center">
          {% if 'ROLE_MEMBER' in app.user.roles %}
            {% set startDate = dateStartMember %}
          {% else %}
            {% set startDate = dateStartAll %}
          {% endif %}
          {% if (startDate|date('Y-m-d') <= "now"|date('Y-m-d')) %}
            {% if (app.user.licenceNumber is empty) %}
              <p class="my-2 bg-warning text-justify">
                <i class="fas fa-exclamation-triangle"></i>
                <strong>
                  Attention, vous n'avez pas renseigné votre numéro de licence.
                </strong>
                Votre inscription sera annulée si vous ne complétez pas votre compte avant le
                {{ dateEnd | date("d/m/Y") }}.
              </p>
              <br>
            {% endif %}
            <p>Souhaitez-vous bien vous inscrire à l'événement : "{{ currentEvent.name }}" ?
            </p>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Annuler</button>
          <form method="post" action="{{ path('subscriptionManager', {'event': currentEvent.id, 'user': app.user.id}) }}">
            <input type="hidden" name="_token" value="{{ csrf_token('subs' ~ currentEvent.id ~ app.user.id) }}">
            <button class="btn btn-primary text-light">Inscription</button>
          </form>
        </div>
      {% else %}
        <p>Vous ne pouvez pas encore vous inscrire à l'événement : "{{ currentEvent.name }}".
          <br>
          Les inscriptions démarrent le
          {{ startDate|date('d/m/Y') }}
          et se terminent le
          {{ dateEnd | date("d/m/Y") }}.
        </p>
      </div>
    </div>
    <div class="modal-footer">
      <button type="button" class="btn btn-primary" data-dismiss="modal">OK</button>
    </div>
  {% endif %}
{% endif %}
</div>
</div>
</div>

<!-- ******** Modal for unsubscription ******** -->
<div class="modal fade" id="unsubsModal" tabindex="-1" role="dialog" aria-labelledby="Unsubscription modal" aria-hidden="true">
<div class="modal-dialog" role="document">
<div class="modal-content">
{% if is_granted('IS_AUTHENTICATED_REMEMBERED')and not isThereSubs is empty %}
  <div class="modal-header">
    <h5 class="modal-title" id="exampleModalLabel">Désinscription</h5>
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>
  <div class="modal-body">
    <div class="container text-center">
      <p>Souhaitez-vous bien vous désinscrire de l'événement : "{{ currentEvent.name }}" ?
      </p>
    </div>
  </div>
  <div class="modal-footer">
    <button type="button" class="btn btn-secondary" data-dismiss="modal">Annuler</button>
    <form method="post" action="{{ path('unsubscriptionManager', {'id': isThereSubs.id}) }}">
      <input type="hidden" name="_token" value="{{ csrf_token('unsubs' ~ isThereSubs.id) }}">
      <button class="btn btn-danger text-light">Désinscription</button>
    </form>
  </div>
{% endif %}
</div>
</div>
</div>
{% endblock %}

{% block bottomScript %}
<!-- This was at the bottom of the body so if calendar doesn't work, move it -->
<!-- Essential or the calendar disappear -->
<script src="js/calendar.js" type="text/javascript"></script>
{# Trying to get the scroll icon function to work made me move this link here: #}
<script src="js/home.js" type="text/javascript"></script>
{% endblock %}
